<%= form_with(model: comment_like) do |form| %>
  <% if comment_like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment_like.errors.count, "error") %> prohibited this comment_like from being saved:</h2>

      <ul>
        <% comment_like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :comment_id, style: "display: block" %>
    <%= form.text_field :comment_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
